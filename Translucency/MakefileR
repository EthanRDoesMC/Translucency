DEBUG = 0


ARCHS = arm64
TARGET_CODESIGN_FLAGS = -SEntitlements.plist

include $(THEOS)/makefiles/common.mk

TWEAK_NAME = TranslucencyParallax
TranslucencyParallax_FILES = Pararelax
TranslucencyParallax_CODESIGN_FLAGS = -SEntitlements.plist
TranslucencyParallax_FRAMEWORKS = UIKit

include $(THEOS_MAKE_PATH)/tweak.mk

after-install::
	install.exec "killall -9 SpringBoard"

